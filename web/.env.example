# Base URL of the PhotoPi API as seen by the browser.
# In the default Docker setup nginx proxies /api to the backend, so a
# relative path works and no value is needed here. Set an absolute URL
# only if you serve the frontend and backend from different origins.
NEXT_PUBLIC_API_BASE=
